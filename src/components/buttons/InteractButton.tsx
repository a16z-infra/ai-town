import React from 'react';
import Button from './Button';
import interactImg from '../../../assets/interact.svg';

export default function InteractButton() {
  return (
    <Button imgUrl={interactImg}>Static Demo</Button>
  );
}